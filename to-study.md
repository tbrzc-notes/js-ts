## Summary
```dataview
table tags as "Etiquetas",date-to-study as "Estudiar el dia" 
from ""
where contains(tags, "summary") and file.name != "plantilla_nota"
sort file.mtime desc
```

## 3-day

```dataview
table tags as "Etiquetas",date-to-study as "Estudiar el dia" 
from ""
where contains(tags, "3-day") and file.name != "plantilla_nota"
sort file.mtime desc
```

## 1-week
```dataview
table tags as "Etiquetas",date-to-study as "Estudiar el dia" 
from ""
where contains(tags, "1-week") and file.name != "plantilla_nota"
sort file.mtime desc
```
## 1-month
```dataview
table tags as "Etiquetas",date-to-study as "Estudiar el dia" 
from ""
where contains(tags, "1-month") and file.name != "plantilla_nota"
sort file.mtime desc
```
## 3-month
```dataview
table tags as "Etiquetas",date-to-study as "Estudiar el dia" 
from ""
where contains(tags, "3-month") and file.name != "plantilla_nota"
sort file.mtime desc
```
## 1-year
```dataview
table tags as "Etiquetas",date-to-study as "Estudiar el dia" 
from ""
where contains(tags, "1-year") and file.name != "plantilla_nota"
sort file.mtime desc
```